import "@testing-library/jest-dom";

// MSW setup is optional - only load when needed
// Tests that need MSW should import and set it up themselves
// This avoids ESM issues with MSW in unit tests that don't need it

